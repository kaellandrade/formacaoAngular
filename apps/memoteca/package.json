{
	"name": "memoteca",
	"version": "0.0.0",
	"scripts": {
		"ng": "ng",
		"start": "ng serve",
		"build": "ng build",
		"watch": "ng build --watch --configuration development",
		"test": "ng test",
		"lint": "ng lint",
		"serve": "json-server --watch ./backend/src/db.json --port 5000"
	},
	"private": true,
	"dependencies": {
		"@formacao/shared-ui": "*"
	}
}
